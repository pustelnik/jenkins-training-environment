jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: admin
          password: training

security:
  globalJobDslSecurityConfiguration:
    useScriptSecurity: false